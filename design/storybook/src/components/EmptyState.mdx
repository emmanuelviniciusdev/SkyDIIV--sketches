import { Canvas, Meta } from '@storybook/addon-docs/blocks'

import * as EmptyStateStories from './EmptyState.stories'

<Meta of={EmptyStateStories} />

# EmptyState

Centered quiet state when there is nothing to show. Lead with a light icon, a short title, and optional supporting copy. One action at most.

<table>
  <thead>
    <tr>
      <th>Part</th>
      <th>Role</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Icon</td>
      <td>light mark above the title</td>
    </tr>
    <tr>
      <td>Title</td>
      <td>what is missing</td>
    </tr>
    <tr>
      <td>Description</td>
      <td>what to do next</td>
    </tr>
    <tr>
      <td>Action</td>
      <td>optional single control</td>
    </tr>
  </tbody>
</table>

<Canvas of={EmptyStateStories.Default} />

## With action

<Canvas of={EmptyStateStories.WithAction} />

## No results

<Canvas of={EmptyStateStories.NoResults} />
