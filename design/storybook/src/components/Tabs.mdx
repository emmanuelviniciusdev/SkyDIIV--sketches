import { Canvas, Meta } from '@storybook/addon-docs/blocks'

import * as TabsStories from './Tabs.stories'

<Meta of={TabsStories} />

# Tabs

A row of paper labels, like permalinks on a quiet page. Inactive stays muted type. The active label is a scrap: stone hairline, page fill. Hover takes the same quiet border and page fill as menu items. No shared bar, no pill track, no elevation. Copy stays short.

<table>
  <thead>
    <tr>
      <th>Part</th>
      <th>Role</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>List</td>
      <td>loose row of labels; no shared hairline</td>
    </tr>
    <tr>
      <td>Trigger</td>
      <td>one section; optional leading icon</td>
    </tr>
    <tr>
      <td>Content</td>
      <td>the active panel</td>
    </tr>
  </tbody>
</table>

<Canvas of={TabsStories.Default} />

## With icons

<Canvas of={TabsStories.WithIcons} />

## On a surface

<Canvas of={TabsStories.OnASurface} />

## Disabled

<Canvas of={TabsStories.Disabled} />
