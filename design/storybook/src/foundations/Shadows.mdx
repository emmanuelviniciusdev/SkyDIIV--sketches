import { Canvas, Meta } from '@storybook/addon-docs/blocks'

import * as ShadowsStories from './Shadows.stories'

<Meta of={ShadowsStories} />

# Shadows

UI does not use drop shadows. Elevation comes from stacking page and surface colors, with a stone-warm border at reduced opacity.

<Canvas of={ShadowsStories.Layers} />
