import { Canvas, Meta } from '@storybook/addon-docs/blocks'

import * as LayoutStories from './Layout.stories'

<Meta of={LayoutStories} />

# Layout

A thin header with a hairline. Quotations are light and italic. Tags use tinted chips. Cards are quiet boxes, not page scaffolding. Whitespace is part of the design.

<Canvas of={LayoutStories.Recipes} />
