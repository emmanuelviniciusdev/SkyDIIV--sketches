import { Canvas, Meta } from '@storybook/addon-docs/blocks'

import * as CursorStories from './Cursor.stories'

<Meta of={CursorStories} />

# Cursor

Moving the pointer leaves a trail of plus signs. The marks sit in slate gray, then fade and grow as they leave. The trail is atmosphere, like paper grain — not a control, not a cursor replacement.

When motion is reduced, there is no trail.

<Canvas of={CursorStories.Trail} />
