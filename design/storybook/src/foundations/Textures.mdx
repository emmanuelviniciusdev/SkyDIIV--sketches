import { Canvas, Meta } from '@storybook/addon-docs/blocks'

import * as TexturesStories from './Textures.stories'

<Meta of={TexturesStories} />

# Textures

Paper grain sits on the page at very low opacity. Surfaces may layer the same grain more strongly. Grain is not a drop shadow.

<Canvas of={TexturesStories.Grain} />
